version: 2
models:
- name: fact_model
  description: 'fact model with all metrics'
  meta:
    label: fact_join
    joins:
      - join: orders  
        sql_on: ${orders.order_id} = ${basket.order_id}
  columns:
  - name:  basket_item_id 
    description: 'basket item id'
    meta:
      metrics:
        count_distinct_basket_item_id:
          type: count_distinct
          label: distinct total basket item